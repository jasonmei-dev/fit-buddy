<%= form_for(:user, url: login_path) do |f| %>
  <%= f.label :name %>
  <%= f.collection_select(:name, User.all, :username, :username) %>
  <%= f.label :password %>
  <%= f.password_field :password, id: 'password' %>
  <%= f.submit 'Log In'%>
<% end %>
